import * as React from 'react'

type notificationProps = {
  isError: boolean
  message: string
}

const HeaderNotification = (props: notificationProps): JSX.Element => {
  return <div>header notification here...</div>
}

export default HeaderNotification
